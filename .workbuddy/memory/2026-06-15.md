# 2026-06-15

## 日常运维 目录初始化与第一篇 deb 文章

- 在 `日常运维/` 下新建 `README.md`，对齐其他目录的模块定位 / 核心主题 / 写作原则四段式写法。
- 撰写并写入 `2026-06-15_deb-端侧镜像与增量更新部署.md`，按 README 规范产出带 frontmatter、≤150 字摘要、命令可复现的技术总结。
  - 知识铺垫：deb 是什么、deb vs apt 关系、deb 内部结构（`debian-binary` / `control.tar.gz` / `data.tar.xz`）、为何端侧选 deb。
  - 工程落地方案：把两份用户资料（RAG母盘镜像与deb部署指南 / 母盘镜像与deb部署指南，核心内容一致）整合为「母盘烘焙 + deb 增量更新」双层范式。
  - 覆盖：步骤 0–6（rsync 模型 → bootstrap clone → setup_runtime_board.sh → 验证 → 测 deb → 删 ~/RAG_V2 → 整盘打镜像）、升级路径、版本号管理、自建 apt 仓库、回滚、7 条避坑点。
- 顺手在项目根 `README.md` 给「运维自动化 / 日常运维」加了一行说明（实际目录名沿用历史 `日常运维/`，内容方向与 README 表一致）。
- 关键结论：母盘负责"在吗"，deb 负责"用哪个版本"；deb 只装业务源码（~1MB），`postinst` 检测到母盘已烘焙 venv 时跳过 pip，是端侧量产唯一可放心批量升级的基线。
