# 2026-07-15 工作日志

## 文章：Cursor Rules 的好处与用法
- 写入 `AI工具思考/2026-07-15_Cursor-Rules的好处与用法.md`
- 主题：Cursor Rules 的两层级（User/Project）、四类型（Always/Auto Attached/Agent Requested/Manual）、`.cursor/rules/*.mdc` 格式与编写原则
- 实战样例：将用户桌面 `cursor_rules.txt`（全中文编码规范：体量死线/命名注释/时区/异常/国内源）整理为可直接落地的 Project Rule（含 frontmatter）与 User Rules 两种用法
- 已同步更新 `AI工具思考/README.md` 文章列表与日期
- 关键事实（2026-07 核实）：扩展名必须 `.mdc` 而非 `.md`；Always 规则成本最高，合计建议 <500 行/2000 token；`/Generate Cursor Rules`、`/create-rule` 可在对话内生成规则
